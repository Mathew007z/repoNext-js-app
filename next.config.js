/** @type {import('next').NextConfig} */
const nextConfig = {
  // para utilizar nueva version de rutas agregar experimental, appDir  = true
  experimental: {
    appDir: true
  },
  reactStrictMode: true,
}

module.exports = nextConfig
